# Video de demo (sección "Pedidos en acción")

Coloca acá los archivos del video que se muestra en la landing, justo después del hero:

- `demo-pedidos.mp4` — el video en sí (usado por `index.html` y `en/index.html`).
- `demo-pedidos-poster.jpg` — imagen de portada opcional (se ve antes de darle play).

Mismo video para ambos idiomas (ES/EN); si quieres versiones separadas por idioma, avisa y se ajustan las rutas en cada HTML.
